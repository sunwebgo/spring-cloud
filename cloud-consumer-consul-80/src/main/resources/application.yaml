server:
  port: 80

spring:
  application:
    name: cloud-consumer-consul-80

  # 配置consul
  cloud:
    consul:
      host: 192.168.26.144
      port: 8500
      discovery:
        service-name: ${spring.application.name}
